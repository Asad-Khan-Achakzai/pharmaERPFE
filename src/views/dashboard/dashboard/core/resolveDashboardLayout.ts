/**
 * @deprecated Use `resolveWidgets` from `../engine/widgetResolver` with `WIDGET_REGISTRY`.
 * Re-exports only for backwards discovery.
 */
export { resolveDashboardMode, resolveWidgets } from '../engine/widgetResolver'
